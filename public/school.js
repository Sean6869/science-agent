let identity=null,csrf='',selectedClass='';
const roleLabel=role=>({admin:'管理员端',teacher:'教师端',student:'学生端'})[role];
const scoped=path=>path+(path.includes('?')?'&':'?')+'class='+encodeURIComponent(selectedClass);
const screen=document.getElementById('schoolScreen');
const e=(tag,text,className)=>{const node=document.createElement(tag);if(text!==undefined)node.textContent=text;if(className)node.className=className;return node;};
export async function apiFetch(url,options={}){
 const response=await fetch(url,{...options,headers:{...options.headers,...(identity?{'x-session-user':identity.id}:{}),...(options.method&&options.method!=='GET'?{'x-csrf-token':csrf}:{})}});
 if((response.status===401&&url!=='/api/auth/login')||response.status===409){location.reload();throw new Error('登录已过期或账号已切换');}return response;
}
async function api(url,data,method='POST'){const r=await apiFetch(url,data===undefined?{}:{method,headers:{'content-type':'application/json'},body:JSON.stringify(data)});const result=await r.json();if(!r.ok)throw new Error(result.message||'操作未完成，请重试');return result;}
function button(label,fn,className='school-button'){const b=e('button',label,className);b.type='button';b.onclick=fn;return b;}
function deleteButton(label,warning,url,refresh){return button(label,async event=>{if(!window.confirm(warning+'\n此操作不可撤销，确定继续吗？'))return;const b=event.currentTarget;b.disabled=true;try{await api(url,{},'DELETE');await refresh();}catch(err){alert(err.message);b.disabled=false;}},'school-link danger');}
function rowActions(...items){const wrap=e('span',undefined,'row-actions');wrap.append(...items);return wrap;}
function message(text,error=false){const n=e('p',text,error?'school-error':'school-note');n.setAttribute('role',error?'alert':'status');return n;}
function header(title,subtitle){const head=e('header',undefined,'school-heading');const logo=e('img');logo.src='/assets/xiaoke-logo-transparent.png?v=20260917';logo.alt='小科';head.append(logo,e('h1',title));if(subtitle)head.append(e('p',subtitle));return head;}
function field(form,label,name,type='text',value='',required=true){const wrap=e('label',undefined,'school-field');wrap.append(e('span',label));const input=e('input');Object.assign(input,{name,type,value,required});if(type==='password'){input.minLength=8;input.maxLength=128;input.autocomplete='new-password';}wrap.append(input);form.append(wrap);return input;}
async function logout(){await api('/api/auth/logout',{});sessionStorage.clear();location.reload();}
function accountBar(){const bar=e('div',undefined,'account-bar');bar.append(e('span',roleLabel(identity.role)),button('退出登录',()=>logout().catch(err=>alert(err.message)),'school-link'));return bar;}
function login(){
 screen.replaceChildren();const card=e('section',undefined,'login-card');card.append(header('欢迎来到科学实验室'));
 const form=e('form');field(form,'账号','username').autocomplete='username';const pass=field(form,'密码','password','password');pass.minLength=1;pass.autocomplete='current-password';
 const submit=e('button','登录','school-button primary');submit.type='submit';const status=message('');form.append(submit,button('教师注册',register,'school-link'),status);card.append(form);screen.append(card);
 form.onsubmit=async event=>{event.preventDefault();submit.disabled=true;status.textContent='正在登录…';try{const result=await api('/api/auth/login',Object.fromEntries(new FormData(form)));csrf=result.csrf;await boot();}catch(err){status.textContent=err.message;status.className='school-error';}finally{submit.disabled=false;}};
}
function register(){
 screen.replaceChildren();const card=e('section',undefined,'login-card');card.append(header('教师注册'));const form=e('form');field(form,'姓名','name');field(form,'班级','className');const username=field(form,'账号','username');username.pattern='[a-zA-Z0-9_.-]{3,40}';field(form,'密码（至少8位）','password','password');const submit=e('button','注册','school-button primary');submit.type='submit';const status=message('');form.append(submit,button('返回登录',login,'school-link'),status);card.append(form);screen.append(card);
 form.onsubmit=async event=>{event.preventDefault();submit.disabled=true;try{const p=Object.fromEntries(new FormData(form));await api('/api/auth/register',p);const auth=await api('/api/auth/login',{username:p.username,password:p.password});csrf=auth.csrf;await boot();}catch(err){status.textContent=err.message;status.className='school-error';submit.disabled=false;}};
}
async function studentTests(){
 const {quizzes,scores}=await api('/api/quizzes');screen.replaceChildren(accountBar());
 const complete=q=>scores.find(s=>s.quizId===q.id&&s.version===q.version);const next=quizzes.find(q=>!complete(q));
 if(!next){const card=e('section',undefined,'school-card');card.append(header('测验已完成','成绩已保存，教师可以在管理端查看。'));for(const q of quizzes)card.append(e('p',`${q.title}：${complete(q).score} / 100 分`));card.append(button('进入实验室',()=>startWorkspace()));screen.append(card);return;}
 const card=e('section',undefined,'school-card quiz-card');card.append(header(next.title,`入门测验 ${quizzes.indexOf(next)+1} / ${quizzes.length} · 每题10分，满分100分`));
 for(const line of next.instructions)card.append(e('p',line,'school-note'));
 if(scores.length)card.append(message(`已保存：${scores.map(s=>`${s.quizId==='lesson-1'?'第一节课':'第二节课'} ${s.score}分`).join('，')}`));
 const form=e('form');const draftKey=`quiz-draft:${identity.id}:${next.id}:${next.version}`;let saved={};try{saved=JSON.parse(sessionStorage.getItem(draftKey)||'{}');}catch{}
 const images=(parent,paths)=>{for(const path of paths){const img=e('img');img.src=path;img.alt='题目配图';img.className='quiz-diagram';parent.append(img);}};
 for(const q of next.questions){const group=e('fieldset');group.append(e('legend',`${q.id}. ${q.text}`));images(group,q.images);for(const option of q.options){const label=e('label',undefined,'quiz-option');const radio=e('input');Object.assign(radio,{type:'radio',name:q.id,value:option.id,required:true,checked:saved[q.id]===option.id});const content=e('span',`${option.id}. ${option.text}`);images(content,option.images);label.append(radio,content);group.append(label);}form.append(group);}
 const progress=message('请完成全部题目；提交后成绩会被保存，不可重复修改。');
 const submit=e('button','提交测验并查看分数','school-button primary');submit.type='submit';form.append(progress,submit);card.append(form);screen.append(card);
 form.onchange=()=>{const answers=Object.fromEntries(new FormData(form));try{sessionStorage.setItem(draftKey,JSON.stringify(answers));}catch{}progress.textContent=`已完成 ${Object.keys(answers).length} / ${next.questions.length} 题`;};
 form.onsubmit=async event=>{event.preventDefault();submit.disabled=true;try{const {result}=await api('/api/quizzes/submit',{quizId:next.id,answers:Object.fromEntries(new FormData(form))});sessionStorage.removeItem(draftKey);card.replaceChildren(header('测验已提交','分数已保存并同步到教师端'),e('p',`${result.score} / 100`,'score-number'),e('p',`答对 ${result.correct} / ${result.total} 题`),button(quizzes.indexOf(next)===0?'继续第二份测验':'查看测验汇总',()=>studentTests().catch(showError)));window.scrollTo(0,0);}catch(err){progress.textContent=err.message;progress.className='school-error';submit.disabled=false;}};
}
function showError(error){screen.append(message(error.message,true));}
function table(columns,rows){const wrap=e('div',undefined,'school-table-wrap'),t=e('table'),thead=e('thead'),tr=e('tr');for(const [label] of columns)tr.append(e('th',label));thead.append(tr);const tbody=e('tbody');for(const row of rows){const line=e('tr');for(const [,key] of columns){const td=e('td');const value=typeof key==='function'?key(row):row[key];if(value instanceof Node)td.append(value);else td.textContent=String(value??'');line.append(td);}tbody.append(line);}t.append(thead,tbody);wrap.append(t);if(!rows.length)wrap.append(message('暂无记录'));return wrap;}
const time=value=>value?new Date(value).toLocaleString('zh-CN'):'';
async function teacher(){
 screen.replaceChildren(accountBar());const card=e('section',undefined,'school-card teacher-card');card.append(header(identity.role==='admin'?'管理员管理':'教师管理'));
 const nav=e('nav',undefined,'school-tabs'),panel=e('section');let selected='students';
 const render=async tab=>{selected=tab;for(const b of nav.children)b.classList.toggle('selected',b.dataset.tab===tab);panel.replaceChildren(message('正在读取记录…'));try{if(tab==='students')await studentsPanel(panel);else if(tab==='scores')await scoresPanel(panel);else if(tab==='teachers')await teachersPanel(panel);else await conversationsPanel(panel);}catch(err){panel.replaceChildren(message(err.message,true));}};
 const tabs=[['students','学生账号'],['scores','测验成绩'],['conversations','AI 对话记录']];if(identity.role==='admin')tabs.unshift(['teachers','教师账号']);
 for(const [key,label] of tabs){const b=button(label,()=>render(key));b.dataset.tab=key;nav.append(b);}nav.append(button('刷新',()=>render(selected),'school-link'));
 if(identity.role==='admin'){const {classes}=await api('/api/teacher/classes');const select=classSelect(classes,true);select.value=selectedClass;select.onchange=()=>{selectedClass=select.value;void render(selected);};card.append(select);}else card.append(e('p',identity.className));
 card.append(nav,panel);screen.append(card);await render('students');
}
function classSelect(classes,all=false){const select=e('select');select.name='classId';select.setAttribute('aria-label','班级');const blank=e('option',all?'全部班级':'请选择班级');blank.value='';select.append(blank);for(const c of classes){const option=e('option',c.name+(c.teacherName?` · ${c.teacherName}（${c.teacherUsername}）`:''));option.value=c.id;select.append(option);}return select;}
async function teachersPanel(panel){
 const {teachers}=await api('/api/admin/teachers');panel.replaceChildren();const editor=e('section',undefined,'student-editor');editor.hidden=true;
 function edit(t){editor.hidden=false;editor.replaceChildren(e('h2','编辑教师'));const form=e('form',undefined,'student-form');for(const [label,key] of [['姓名','name'],['班级','className'],['账号','username']])field(form,label,key,'text',t[key]);field(form,'新密码（留空则不修改）','password','password','',false);const active=e('input');active.type='checkbox';active.checked=t.active;const activeLabel=e('label','启用账号');activeLabel.prepend(active);form.append(activeLabel);const submit=e('button','保存修改','school-button primary');submit.type='submit';const status=message('');form.append(submit,button('取消',()=>editor.hidden=true,'school-link'));editor.append(form,status);form.onsubmit=async event=>{event.preventDefault();submit.disabled=true;try{await api('/api/admin/teachers/'+t.id,{...Object.fromEntries(new FormData(form)),active:active.checked},'PUT');await teachersPanel(panel);}catch(err){status.textContent=err.message;submit.disabled=false;}};}
 const passwordCell=t=>{if(!t.password)return e('span','未保存');const wrap=e('span',undefined,'password-cell'),value=e('span','••••••••');let visible=false;wrap.append(value,button('显示',event=>{visible=!visible;value.textContent=visible?t.password:'••••••••';event.currentTarget.textContent=visible?'隐藏':'显示';},'school-link'));return wrap;};
 panel.append(editor,table([['姓名','name'],['班级','className'],['账号','username'],['密码',passwordCell],['状态',t=>t.active?'启用':'停用'],['操作',t=>rowActions(button('编辑 / 重置密码',()=>edit(t),'school-link'),deleteButton('删除',`将删除教师“${t.name}”的账号，教师将无法登录。班级、学生账号、成绩和学生对话记录仍由管理员保留。`,'/api/admin/teachers/'+t.id,()=>teachersPanel(panel)))]],teachers));
}
async function studentsPanel(panel){
 const [{students},{classes}]=await Promise.all([api(scoped('/api/teacher/students')),api('/api/teacher/classes')]);panel.replaceChildren();const editor=e('section',undefined,'student-editor');
 function edit(student){editor.replaceChildren(e('h2',student?'编辑学生账号':'创建学生账号'));const form=e('form',undefined,'student-form');field(form,'姓名','name','text',student?.name||'');if(identity.role==='admin'){const select=classSelect(classes);select.value=student?.classId||selectedClass;form.append(select);const newClass=field(form,'新建班级','className','text','',!select.value);const sync=()=>{newClass.parentElement.hidden=!!select.value;newClass.required=!select.value;};select.onchange=sync;sync();}field(form,'年龄','age','number',student?.age||'',false);const label=e('label',undefined,'school-field');label.append(e('span','性别'));const select=e('select');select.name='gender';for(const value of ['未填写','男','女']){const option=e('option',value);option.value=value;option.selected=student?.gender===value;select.append(option);}label.append(select);form.append(label);const username=field(form,'账号','username','text',student?.username||'');username.pattern='[a-zA-Z0-9_.-]{3,40}';field(form,student?'新密码（留空则不修改）':'初始密码（至少8位）','password','password','',!student);
 const activeLabel=e('label',undefined,'school-field');const active=e('input');active.type='checkbox';active.checked=student?.active!==false;activeLabel.append(active,e('span','启用账号'));if(student)form.append(activeLabel);
 const submit=e('button',student?'保存修改':'创建账号','school-button primary');submit.type='submit';const status=message('');form.append(submit);if(student)form.append(button('取消编辑',()=>edit(null),'school-link'));editor.append(form,status);
 form.onsubmit=async event=>{event.preventDefault();submit.disabled=true;try{const values={...Object.fromEntries(new FormData(form)),active:active.checked};await api(student?`/api/teacher/students/${student.id}`:'/api/teacher/students',values,student?'PUT':'POST');await studentsPanel(panel);}catch(err){status.textContent=err.message;status.className='school-error';submit.disabled=false;}};
 }
 const batch=e('section',undefined,'student-editor');batch.append(e('h2','批量导入学生'));const upload=e('input');upload.type='file';upload.accept='.xlsx';upload.setAttribute('aria-label','学生Excel文件');const status=message(''),actions=e('div',undefined,'school-tabs');let classChoice;if(identity.role==='admin'){classChoice=classSelect(classes);classChoice.value=selectedClass;batch.append(classChoice);}const importButton=button('导入Excel',async()=>{const file=upload.files[0];if(!file){status.textContent='请选择Excel文件';return;}if(file.size>1024*1024){status.textContent='文件不能超过1MB';return;}if(classChoice&&!classChoice.value){status.textContent='请选择班级';return;}importButton.disabled=true;try{const data=await new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(reader.result.split(',')[1]);reader.onerror=reject;reader.readAsDataURL(file);});const result=await api('/api/teacher/import',{data,classId:classChoice?.value});await studentsPanel(panel);panel.prepend(message(result.alreadyImported?'此文件已导入，可直接导出账号。':`已创建 ${result.created} 个学生账号`));}catch(err){status.textContent=err.message;importButton.disabled=false;}});const template=e('a','下载模板','school-link');template.href='/student-template.xlsx';template.download='student_information.xlsx';const download=e('a','导出账号密码 Excel','school-button');download.href=scoped('/api/teacher/accounts.xlsx');actions.append(importButton,template,download);batch.append(upload,actions,status);
 edit(null);panel.append(batch,editor,e('h2',`学生列表 · ${students.length}人`),table([['姓名','name'],['性别','gender'],['年龄','age'],['班级','className'],['账号','username'],['状态',s=>s.active?'启用':'停用'],['操作',s=>rowActions(button('编辑 / 重置密码',()=>{edit(s);editor.scrollIntoView({behavior:'smooth'});},'school-link'),deleteButton('删除',`将删除学生“${s.name}”的账号、全部测验成绩和AI对话记录。`,'/api/teacher/students/'+s.id,()=>studentsPanel(panel)))]],students));
}
function exportLink(type,student=''){const a=e('a','导出记录','school-button');a.href=scoped(`/api/teacher/export?type=${type}&student=${encodeURIComponent(student)}`);return a;}
async function scoresPanel(panel){const [{students},{scores}]=await Promise.all([api(scoped('/api/teacher/students')),api(scoped('/api/teacher/scores'))]);panel.replaceChildren(exportLink('scores'));const rows=students.flatMap(s=>[1,2].map(number=>{const result=scores.find(r=>r.studentId===s.id&&r.quizId===`lesson-${number}`);return {...s,lesson:`第${number}节课`,score:result?`${result.score} / 100`:'未完成',createdAt:time(result?.createdAt)};}));panel.append(table([['姓名','name'],['班级','className'],['账号','username'],['测验','lesson'],['分数','score'],['提交时间','createdAt'],['操作',s=>deleteButton('清除测验',`将清除学生“${s.name}”两份测验的全部成绩，该学生需要重新完成入门测验。`,'/api/teacher/students/'+s.id+'/scores',()=>scoresPanel(panel))]],rows));}
async function conversationsPanel(panel){
 const {students}=await api(scoped('/api/teacher/students'));panel.replaceChildren();const filters=e('div',undefined,'school-tabs'),select=e('select');select.setAttribute('aria-label','筛选学生');const all=e('option','全部学生');all.value='';select.append(all);for(const s of students){const option=e('option',`${s.className} · ${s.name}（${s.username}）`);option.value=s.id;select.append(option);}const list=e('section'),pager=e('div',undefined,'school-tabs');let page=0;const exportArea=e('span');filters.append(select,exportArea);panel.append(filters,list,pager);
 const load=async()=>{list.replaceChildren(message('正在加载…'));pager.replaceChildren();exportArea.replaceChildren(exportLink('conversations',select.value));try{const {conversations}=await api(scoped(`/api/teacher/conversations?student=${encodeURIComponent(select.value)}&page=${page}`));list.replaceChildren();if(!conversations.length)list.append(message('暂无记录'));for(const row of conversations){const item=e('article',undefined,'conversation-record');item.append(e('h3',`${row.name} · ${row.className} · ${row.agent==='knowledge'?'知识答疑':'元认知支架'}`),e('small',`${time(row.createdAt)} · ${row.lessonId} ${row.stage?`· 环节${row.stage}`:''} · ${row.source==='model'?'DeepSeek在线回答':row.source==='fallback'?'离线备用回复':row.source==='rule'?'流程提示':row.status}`),e('h4','学生'),e('p',row.userText),e('h4','AI'),e('p',row.reply||'未收到回复'),deleteButton('删除对话',`将删除“${row.name}”这条提问及对应的AI回答。`,'/api/teacher/conversations/'+row.id,()=>load()));list.append(item);}const prev=button('上一页',()=>{page--;void load();});prev.disabled=page===0;const next=button('下一页',()=>{page++;void load();});next.disabled=conversations.length<100;pager.append(prev,e('span',`第${page+1}页`),next);}catch(err){list.replaceChildren(message(err.message,true));}};
 select.onchange=()=>{page=0;void load();};await load();
}
async function startWorkspace(){screen.hidden=true;document.body.classList.add('workspace-ready');window.schoolUser=identity;document.querySelector('.app').hidden=false;const bar=accountBar();bar.classList.add('workspace-account');document.querySelector('.panel-head').after(bar);await import('./app.js');}
async function boot(){const session=await api('/api/me');if(!session.user){login();return;}identity=session.user;csrf=session.csrf;window.schoolUser=identity;if(identity.role!=='student')await teacher();else if(session.ready)await startWorkspace();else await studentTests();}
boot().catch(error=>{screen.replaceChildren(header('暂时无法连接','请确认服务已启动后重试'),message(error.message,true),button('重新连接',()=>location.reload()));});
